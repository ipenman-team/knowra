'use client';

import { PageContainer } from '@/components/page';

export function SpacePagesView(props: { spaceId: string }) {
  return (
    <div className="min-h-0">
      <PageContainer />
    </div>
  );
}
